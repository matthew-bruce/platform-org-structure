# 🚀 ROYAL MAIL ORG CHART V2.4 - COMPLETE PACKAGE

## ✅ WHAT'S IN THIS PACKAGE:

This is a **COMPLETE** rebuild with all your V2.4 requirements:

### ✨ NEW FEATURES:
1. ✅ 3-Level Structure: Platforms (L3) > Initiatives (L2) > Teams (L1)
2. ✅ Matthew Bruce at very top (separate from Leadership)
3. ✅ Fixed drag-drop (reorder within team, to/from bench)
4. ✅ Team management (edit, delete, move)
5. ✅ Duplicate button (splits capacity 50/50)
6. ✅ Shared SMEs (renamed from Tower SMEs)
7. ✅ Bench pushes content up (not overlay)
8. ✅ Normal view shows: Full name, Title, Capacity
9. ✅ Leadership renamed (was "Leadership & Governance")
10. ✅ New teams: Cygnus, Cosmos, Nebula, Helios, Janus, Orion
11. ✅ New initiatives: Technology Transformation, Strategic Demand, Care & Maintenance, Legacy Demand, Ensono Exit

---

## 📦 FILES INCLUDED:

### SQL:
- `sql/schema-v2.4-FIXED.sql` - Complete 3-level database

### TypeScript:
- `lib/types.ts` - Updated types (Platform, Initiative, Team, Person)

### Components (7 files):
- `components/PersonCard.tsx` - Shows full name in normal view
- `components/Sidebar.tsx` - Duplicate button, "Shared SME" label  
- `components/BenchDrawer.tsx` - Inline (not fixed)
- `components/InitiativeContainer.tsx` - NEW (replaces TowerContainer)
- `components/OrgChart.tsx` - 3-level rendering
- `app/page.tsx` - Complete state management for 3 levels

### Instructions:
- `V2.4_README.md` - This file
- `DEPLOY_INSTRUCTIONS.md` - Step-by-step guide

---

## 🚀 DEPLOYMENT (20 min):

### Step 1: Database (DONE ✅)
You already ran `schema-v2.4-FIXED.sql` in Supabase

### Step 2: Copy Files to GitHub (15 min)

Replace these files in your `platform-org-structure` repo:

**lib/**
- `lib/types.ts`

**components/** 
- `components/PersonCard.tsx`
- `components/Sidebar.tsx`
- `components/BenchDrawer.tsx`
- `components/InitiativeContainer.tsx` (NEW FILE - create it)
- `components/OrgChart.tsx`

**app/**
- `app/page.tsx`

**NOTE:** Delete old `components/TowerContainer.tsx` if it exists

### Step 3: Deploy
- Push to GitHub
- Vercel auto-deploys
- Done!

---

## ⚠️ IMPORTANT NOTES:

### Files NOT in Package (Keep Your Existing):
- `lib/supabase.ts` - NO CHANGE
- `components/AdminMode.tsx` - NO CHANGE
- `app/layout.tsx` - NO CHANGE
- `app/globals.css` - NO CHANGE
- All config files (package.json, etc.) - NO CHANGE

### What Changed:
- `towers` table → `initiatives` table
- `tower_id` → `initiative_id` everywhere
- `is_tower_sme` → `is_shared_sme`
- `TowerContainer.tsx` → `InitiativeContainer.tsx`

---

## 🎯 AFTER DEPLOYMENT:

You'll see:
1. Matthew Bruce alone at top with red border
2. Leadership section below (6 people)
3. Strategic Platform with 2 initiatives
4. Legacy Platform with 3 initiatives
5. Teams scroll horizontally within each initiative
6. Shared SMEs at bottom of each initiative
7. Bench drawer at bottom (expands/collapses)

---

## 🐛 IF SOMETHING BREAKS:

1. Check browser console for errors
2. Verify all files copied correctly
3. Make sure you deleted old `TowerContainer.tsx`
4. Check Supabase has `initiatives` table (not `towers`)

---

Ready to deploy! All files are in this package.
